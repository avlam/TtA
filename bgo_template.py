# Placeholder file to show what is expected withing bgo.py. Replace with actual credentials when using on local machine, but should find a way to handle security more robustly moving forward.

login_data = {
    'identifiant': 'username', # username
    'mot_de_passe': 'password' #password
}